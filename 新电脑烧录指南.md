# ARCS Mini 固件烧录指南（新电脑零配置指南）

本文档教你如何在**一台全新的 Windows 电脑**上，无需搭建任何开发编译环境（无需安装 GCC、Python、CMake 等），直接烧录固件到设备中。

---

## 一、发给别人的文件清单（烧录工具包）

如果你要打包发给别人，只需要提取以下目录和文件，打成一个 zip 压缩包即可（**无需发送完整的源码和 SDK**，体积小、安全且快速）：

```text
烧录工具包/
├── 一键烧录.bat            （双击完整烧录：静态资源 + App 固件）
├── 只更新固件.bat          （双击仅烧录：App 固件，速度最快）
├── adb_download.ps1       （后台烧录自动化脚本）
├── tools/
│   └── adb/
│       ├── adb.exe
│       ├── AdbWinApi.dll
│       └── AdbWinUsbApi.dll
├── res/
│   └── arcs-mini/
│       ├── partition_table.json
│       ├── ap.bin
│       ├── tone.bin
│       ├── wake_word.bin
│       ├── emoji.bin
│       └── respak.bin
└── build/
    └── arcs-mini.bin      （最新的应用程序固件，如 SM-2026-002）
```

---

## 二、新电脑烧录步骤

### 1. 硬件连接与准备
1. 准备一根 **Type-C 数据线**（⚠️ 必须支持数据传输，不要使用只能充电的线）。
2. 将数据线一头插入电脑 USB 接口，另一头插入设备。
3. 将设备**正常开机**（屏幕亮起或指示灯正常工作）。

### 2. 开始烧录
1. 解压收到的压缩包到任意英文路径（例如 `D:\flash_pack`）。
2. **首次全量烧录 / 更换新板子**：
   - 鼠标右键或直接双击运行 **`一键烧录.bat`**。
3. **日常仅更新功能固件**：
   - 鼠标双击运行 **`只更新固件.bat`**。
4. 脚本会自动完成以下操作：
   - 检测 ADB 工具及已连接设备。
   - 自动让设备进入 Recovery 下载模式（设备会显示为 `BOOT-xxxx`）。
   - 自动分发并推送镜像分区到 Flash。
   - 烧录完成后自动触发设备软重启。
5. 看到窗口提示：
   ```text
   Flash summary:
   - success: 1
     [OK] BOOT-xxxxxxxx
   All devices flashed successfully
   [成功] 固件烧录完成！设备正在重启...
   ```
   即代表烧录圆满成功！

---

## 三、常见问题与排查指南

### 1. 提示 `Error: no usable devices detected`
- **原因 1：线缆问题**。大部分问题是因为用了只能充电、不能传输数据的 Type-C 充电线，更换优质手机数据线重试。
- **原因 2：未开机**。确保设备处于开机状态。
- **原因 3：USB 接触不良**。插拔一次，或尝试插到电脑机箱后置的 USB 端口。

### 2. 设备管理器提示未知设备 / 驱动黄色感叹号
- Windows 10/11 绝大多数情况下会自动匹配 WinUSB 驱动。
- 如果设备进入 Boot 模式（如出现 `BOOT-xxxx`）后识别不到：
  - 可以下载轻量驱动安装工具 **Zadig** (https://zadig.akeo.ie/)。
  - 打开 Zadig，在下拉菜单中找到带有 `BOOT-` 或 ListenAI 的设备，将驱动选择为 `WinUSB`，点击 **Replace Driver** 即可。

### 3. PowerShell 执行策略限制提示 (`ExecutionPolicy`)
- 本工具包的 `.bat` 文件默认已包含 `-ExecutionPolicy Bypass` 参数，正常情况下双击不会被策略拦截。
- 若在单独 PowerShell 命令行运行报错，可先在 PowerShell 窗口执行：
  ```powershell
  Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
  ```
