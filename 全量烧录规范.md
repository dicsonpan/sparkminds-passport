# ARCS-MINI 固件烧录铁律与全量规范

> **核心原则**：本设备架构依赖多核协同与外部 Flash 资源包，**任何情况下都严禁只单独烧录 0x600000 APP 分区**！
> 缺少字库、UI 资源（respak）或引导程序会导致系统无法渲染、开机黑屏。**每次烧录必须全量烧录所有 7 个分区！**

---

## 一、Flash 全量分区映射表

| 分区序号 | 起始物理地址 | 镜像文件路径 | 分区作用说明 |
| :--- | :--- | :--- | :--- |
| **1** | `0x00000000` | `res\arcs-mini\boot.bin` | 底层引导 Bootloader（开机键守护与阶段0引导） |
| **2** | `0x00040000` | `res\arcs-mini\ap.bin` | 协处理器底层核心固件 |
| **3** | `0x00100000` | `res\arcs-mini\tone.bin` | 系统提示音资源包 |
| **4** | `0x00200000` | `res\arcs-mini\wake_word.bin` | 语音识别唤醒底模 |
| **5** | `0x00380000` | `res\arcs-mini\emoji.bin` | 动效表情资源 |
| **6** | `0x00440000` | `res\arcs-mini\respak.bin` | **核心 UI 资源包（字库、图标、图片，黑屏克星）** |
| **7** | `0x00600000` | `build\arcs-mini.bin` | **当前学员应用程序主固件** |

---

## 二、官方推荐标准烧录命令（串口 COM4）

执行以下命令即可一次性完整写入全部分区并自动复位开机：

```powershell
.\tools\cskburn\cskburn.exe -C arcs -s COM4 -b 1500000 --probe-timeout 45000 0x000000 res\arcs-mini\boot.bin 0x040000 res\arcs-mini\ap.bin 0x100000 res\arcs-mini\tone.bin 0x200000 res\arcs-mini\wake_word.bin 0x380000 res\arcs-mini\emoji.bin 0x440000 res\arcs-mini\respak.bin 0x600000 build\arcs-mini.bin
```

或者使用打包生成的复合 Hex 直接烧录前 6 个分区加 Bootloader：
```powershell
.\tools\cskburn\cskburn.exe -C arcs -s COM4 -b 1500000 0x000000 res\arcs-mini\boot.bin build\arcs-mini.combined.hex
```

---

## 三、一键双击脚本
根目录下已配置好 **`全量串口烧录.bat`**，直接双击运行即可自动完成上述全部流程。
