# ARCS Mini 固件烧录指南（新电脑零配置指南）

本文档教你如何在**一台全新的 Windows 电脑**上，无需安装任何开发编译环境（无需 GCC、Python、CMake 等），直接将固件烧录到设备中。

---

## 1. 硬件准备
1. 准备一根 **Type-C 数据线**（⚠️ 必须支持数据传输，切勿使用只能充电的慢充线）。
2. 将数据线一头插在电脑 USB 口，另一头插在设备上。
3. 将设备**开机**（屏幕或指示灯点亮）。

---

## 2. 一键烧录步骤
1. 将收到的烧录压缩包解压到电脑本地（建议解压到无中文/无空格的路径，如 `D:\flash_tool`）。
2. **首次烧录 / 新设备**：
   - 鼠标双击运行 **`flash_all.bat`**（烧录静态资源 + 应用程序固件）。
3. **日常仅更新功能固件**：
   - 鼠标双击运行 **`flash_app.bat`**（仅烧录应用程序固件，速度最快）。
4. 脚本会自动完成以下动作：
   - 自动检测并调用自带的 ADB 工具与设备通讯。
   - 自动让设备进入 Recovery 下载模式（设备会显示为 `BOOT-xxxx`）。
   - 自动推送固件分区并校验。
   - 烧录完毕后自动重启设备。
5. 当黑框窗口最后显示：
   ```text
   Flash summary:
   - success: 1
     [OK] BOOT-xxxxxxxx
   All devices flashed successfully
   [SUCCESS] Flashing completed! Device is rebooting...
   请按任意键继续...
   ```
   即说明烧录成功，设备已刷入最新固件并重启！

---

## 3. 常见问题排查

### Q1：提示 `Error: no usable devices detected`
- **排查 1**：极大概率是数据线为“纯充电线”，请更换原装手机数据线重试。
- **排查 2**：设备没有开机，请长按电源键开机后再运行脚本。
- **排查 3**：插在机箱前置 USB 口供电不足，建议换到机箱背面的 USB 口。

### Q2：设备管理器出现黄色感叹号 / 识别不到 BOOT 设备
- Windows 10/11 通常会自动匹配 WinUSB 驱动。
- 如遇个别精简版系统无驱动，可下载免费驱动工具 [Zadig](https://zadig.akeo.ie/)，下拉选中带 `BOOT-` 的设备，安装为 `WinUSB` 驱动即可。
